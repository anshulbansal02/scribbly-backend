class Avatar {
    constructor(accentColor, imageURI) {
        this.accentColor;
        this.imageURI;
    }

    static async create(seed) {
        let accent;
        let uri;
        // Fetch the avatar image
        // Find accent color;
        return new Avatar(accent, uri);
    }
}

export default Avatar;
